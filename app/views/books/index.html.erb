<!-- <section id="index">
        <h1>Liste des livres</h1>
        <div class="container">
            <div class="flex-end" >
            <a href="/books/create" class="btn btn-sm btn-primary float-right">Nouveau</a>
            </div>
        </div>

        <div class="container my-5">

                <table class="table table-dark table-striped">

                  <thead>
                    <tr>
                      <th>Id</th>
                      <th>Titre</th>
                      <th>Auteur</th>
                      <th>Genre</th>
                      <th>Annee_publication</th>
                      <th>Exemplaires_disponibles</th>
                      <th>Isbn</th>
                      <th>Image</th>
                       <th>Actions</th>
                    </tr>
                  </thead>
                  <tbody>
                        <% @books&.each do |book| %>
                          <tr>

                                <td><%= book.titre %></td>
                                <td><%= book.auteur %></td>
                                <td><%= book.genre %></td>
                                <td><%= book.annee_publication %></td>
                                <td><%= book.exemplaires_disponibles %></td>
                                <td><%= book.isbn %></td>
                                <td class="text-center">
                                      <%= image_tag(@book.image, alt: "", width: "40", height: "50", class: "rounded-circle") if @book && @book.image.present? && File.exist?("app/assets/images/books/#{@book.image}") %>
                                      <%= image_tag("assets/images/", alt: "", width: "40", height: "40", class: "rounded-circle") if @book && (@book.image.blank? || !File.exist?("app/assets/images/books/#{@book.image}")) %>
                                </td>

                                <td class="text-center">
                                    <%= link_to 'Destroy', destroy_book_path(@book), method: :delete, data: { confirm: 'Are you sure you want to delete this book?' }, class: "btn btn-sm btn-outline-danger" %>
                                </td>

                          </tr>
                        <% end %>
                  </tbody>
                </table>


        </div>



        </div>

    </section> #about -->
    <script>
        function deletePatient(id) {
            if(confirm('Etes vous sure de vouloir supprimer cette enregistrement ?')){
                document.getElementById('delete-' + id).submit();
            }
        }
    </script>

<h1>Books</h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <strong><%= book.titre %></strong> by <%= book.id %>
      <p><%= book.auteur %></p>
      <p><%= book.genre %></p>
      <p><%= book.annee_publication %></p>
      <p><%= book.exemplaires_disponibles %></p>
      <p><%= book.isbn %></p>
      <p><%= book.image %></p>
    </li>
  <% end %>
</ul>
<%= link_to 'New Book', new_book_path %>
<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
